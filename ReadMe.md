###This Repo's purpose is to build a network (our in class lab machines) to run a local ai model. When anyone tries to talk to this model they could be really talking to any of these backends on any machine in the class. Everybody will have something similar to this repo on their machines in this class. 

We will need an interface to talk to a agent (running on other lab machines)
will need some way of knowing which machine's agent we are talking to. (OpenWebUI)/ Prometheus / Grafana and some service to monitor the cpu/gpu


